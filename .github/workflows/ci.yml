name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: tests (py${{ matrix.python }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python: ["3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}
          cache: pip
      - name: Install
        run: |
          python -m pip install -q --upgrade pip
          pip install -q -r requirements.txt -r requirements-agent.txt
      - name: Run tests
        run: pytest -q
        # No API key and no GPU in CI: every optional backend has a
        # deterministic fallback, so the whole suite still runs.

  postgres:
    name: postgres + pgvector backend
    runs-on: ubuntu-latest
    services:
      db:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: rag
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres -d rag"
          --health-interval 5s --health-timeout 5s --health-retries 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: |
          python -m pip install -q --upgrade pip
          pip install -q -r requirements.txt -r requirements-pg.txt
      - name: Backend tests
        env:
          PG_TEST_DSN: postgresql://postgres:postgres@localhost:5432/rag
        run: pytest tests/test_pg_store.py -q

  quality-gate:
    # The system has two honest configurations, so gate both: the zero-dependency
    # fallback backends and the real sentence-transformers + cross-encoder stack.
    # check_thresholds picks the matching profile from the report's embedder.
    name: quality gate (${{ matrix.profile }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - profile: fallback
            embed: tfidf
            rerank: lexical
          - profile: ml
            embed: st
            rerank: cross_encoder
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install
        run: |
          python -m pip install -q --upgrade pip
          pip install -q -r requirements.txt
      - name: Install ML backends
        if: matrix.profile == 'ml'
        run: pip install -q -r requirements-ml.txt --extra-index-url https://download.pytorch.org/whl/cpu
      - name: Cache embedding + reranker models
        if: matrix.profile == 'ml'
        uses: actions/cache@v4
        with:
          path: ~/.cache/huggingface
          key: hf-${{ hashFiles('requirements-ml.txt', '.env.example') }}
      - name: Evaluate retrieval against the frozen gold set
        env:
          EMBED_BACKEND: ${{ matrix.embed }}
          RERANK_BACKEND: ${{ matrix.rerank }}
        run: python -m eval.run_eval --docs data/sample_docs --out-json eval/reports/ci.json
      - name: Enforce thresholds
        # Fails the build if recall@5 / MRR / nDCG regress below eval/thresholds.json.
        run: python -m eval.check_thresholds eval/reports/ci.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: eval-report-${{ matrix.profile }}
          path: eval/reports/ci.json

  frontend:
    name: frontend build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - run: npm ci
        working-directory: frontend
      - run: npm run build
        working-directory: frontend
